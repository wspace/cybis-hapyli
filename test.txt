import "stdlib.hpl"
import "string.hpl"

var *a = 123            ;; Setup variables
var *b = 0x0A
var *c = (1 2 3)
var *d = "123"
var *e = 'x'

;; This is a comment

asm test (a b c) = (
    push 4
    swap
    slide 5
    ret
)

;;; testing let-forms

def main () = 
    let 
        x = 1
        y = x
        z = (+ x y)
    in 
        (do (print-string "Hello, world!")
            (print-number (+ x (+ y z))))

;;; Test string replacements:

var *hello1 = ('h', 'e', 'l', 'l', 'o', '\0')

var *hello2 = "Hello\tworld!\nThis is a test!"